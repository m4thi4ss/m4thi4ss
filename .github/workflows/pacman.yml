name: 🟡 Pac-Man Commit Eater

on:
  schedule:
    - cron: "0 0 * * *"  # roda todo dia à meia-noite UTC
  workflow_dispatch:       # permite rodar manualmente

jobs:
  commit-eater:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do seu repositório
        uses: actions/checkout@v3

      - name: Instalar dependências
        run: |
          sudo apt-get update
          sudo apt-get install -y curl unzip nodejs npm

      - name: Baixar o repositório commit-eater como ZIP
        run: |
          curl -L https://github.com/LeoMorais0930/commit-eater/archive/refs/heads/main.zip -o commit-eater.zip
          unzip commit-eater.zip
          cd commit-eater-main
          npm install
          npm run generate
          cp dist/pacman.svg ../pacman.svg

      - name: Commit & Push do pacman.svg
        uses: EndBug/add-and-commit@v9
        with:
          author_name: github-actions
          author_email: github-actions@github.com
          message: "🎮 Atualiza animação Pac-Man"
          add: "pacman.svg"
