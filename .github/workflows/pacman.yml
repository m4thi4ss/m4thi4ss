name: 🟡 Pac-Man Commit Eater

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  commit-eater:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do seu repositório
        uses: actions/checkout@v3

      - name: Instalar Git e Node.js
        run: |
          sudo apt-get update
          sudo apt-get install -y git curl
          curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
          sudo apt-get install -y nodejs

      - name: Clonar repositório commit-eater
        run: |
          git clone https://github.com/LeoMorais0930/commit-eater.git
          cd commit-eater
          npm install
          npm run generate
          cp dist/pacman.svg ../pacman.svg

      - name: Commit & Push do pacman.svg
        uses: EndBug/add-and-commit@v9
        with:
          author_name: github-actions
          author_email: github-actions@github.com
          message: "🎮 Atualiza animação Pac-Man"
          add: "pacman.svg"
